@if (Request.IsAuthenticated)
{
    <text>
        <ul class="nav navbar-nav navbar-right">
            <li class="navbar-text">
                Hallo @User.Identity.Name!
            </li>
            <li>
                @Html.ActionLink("Abmelden", "SignOut", "Account")
            </li>
        </ul>
    </text>
}
else
{/*
    <ul class="nav navbar-nav navbar-right">
        <li>@Html.ActionLink("Anmelden", "SignIn", "Account", routeValues: null, htmlAttributes: new { id = "signLink" })</li>
    </ul>
  */
}